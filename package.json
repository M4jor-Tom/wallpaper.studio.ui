{
  "name": "svg-studio-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "types": ": \"${BGSVG_WASM:?BGSVG_WASM is unset -- run inside \\`nix develop\\`}\" && mkdir -p vendor-types && cp -f $BGSVG_WASM/web/bgsvg_wasm.d.ts vendor-types/bgsvg_wasm.d.ts",
    "dev": "vite",
    "build": "bun run types && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "bun test",
    "drift": "bun run tools/drift.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
