{
  "name": "svg-studio-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "types": ": \"${BGSVG_WASM:?BGSVG_WASM is unset -- run inside \\`nix develop\\`}\" && mkdir -p vendor && cp -f $BGSVG_WASM/web/bgsvg_wasm.d.ts $BGSVG_WASM/web/bgsvg_wasm.js $BGSVG_WASM/web/bgsvg_wasm_bg.wasm vendor/",
    "dev": "vite",
    "build": "bun run types && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "bun run types && bun test",
    "drift": "bun run tools/drift.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
